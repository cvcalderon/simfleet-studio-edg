# MAIN — notificación C-IMPL-01

Se ha preparado el scaffold del repositorio PRE-F3 conforme a la decisión vigente de Parte C:
`Python-library-first + Jupyter científico + Proxmox para ejecución pesada`.

**Estado ahora:** `C-IMPL-01 = PACKAGE_READY; VM_VERIFICATION_PENDING`.

Entregado: proyecto Python 3.12 importable, directorios `src/configs/tests/notebooks`, script de comprobación, pyproject, instrucciones PyCharm/Git y prueba smoke. La ejecución/instalación en la VM del usuario y el primer commit están pendientes de evidencia.

**No registrar** `PRE-F3 BASELINE REPRODUCED`. R0–R7 siguen PENDIENTES. No cambia ninguna decisión F0–F2 ni se comienza F3/D_GEN.

**Próxima evidencia esperada del Worker:** salida de `verify_scaffold.py`, `pytest`, `ruff`, versión de Python, `git status --short` y `git rev-parse HEAD` después del primer commit.
