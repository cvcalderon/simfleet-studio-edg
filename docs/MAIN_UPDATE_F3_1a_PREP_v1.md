# MAIN update — F3.1a preparation v1

F3.1a freezes the D_GEN causal/runtime contract before model selection.

Proposed component order:

`PARTICIPATION -> TRIP_COUNT -> ACTIVITY_CHAIN -> TIME_SCHEDULE -> DISTANCE_PRIOR`

Runtime may consume only M1 state, scenario-known context, frozen artifacts and already-generated upstream D_GEN state. Realised source outcomes remain fit/validation evidence only. Exact destination/route are M3; mode is M5.

Machine-readable contract, feature registry, target mapping, decision register and 24 boundary invariants are included. Model-family choice and CAL tuning are explicitly deferred to F3.1b.

After repository verification passes, close `F3.1a = SUPERADO` and open `F3.1b`.
