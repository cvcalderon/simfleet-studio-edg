# F3.2a local validation v1

Preparation-side static validation:

- contract verifier: expected PASS;
- focused tests: 8/8 PASS in preparation container;
- Python compile: PASS;
- source empirical support calculations used TRAIN/CAL only; TEST outcome rows were not selected;
- Ruff must be confirmed in the official VM before commit.

This validates the freeze package structure, not yet the future materializer output bytes.
